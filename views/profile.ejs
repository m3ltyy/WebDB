<!DOCTYPE html>
<html>
<head>
    <title>Профиль</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <h1>Профиль пользователя</h1>
        <% if (user.profilePicture) { %>
            <img src="<%= user.profilePicture %>" alt="Profile Picture" style="width: 150px; height: 150px; border-radius: 50%;">
        <% } %>
        <p>Имя пользователя: <%= user.username %></p>
        <p>Email: <%= user.email %></p>
        <a href="/profile/edit" class="btn">Редактировать профиль</a>
        <form action="/profile/upload" method="POST" enctype="multipart/form-data">
            <input type="file" name="profilePicture" accept="image/*" required>
            <button type="submit">Загрузить фото</button>
        </form>
        <form action="/profile/delete" method="POST" onsubmit="return confirm('Вы уверены, что хотите удалить профиль?');">
            <button type="submit" class="delete-btn">Удалить профиль</button>
        </form>
        <a href="/dashboard" class="btn back-btn">Назад</a>
    </div>
</body>
</html> 